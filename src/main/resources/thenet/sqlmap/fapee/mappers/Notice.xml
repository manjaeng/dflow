<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.thenet.fapee.notice.service.impl.NoticeMapper">
	
	<select id="selectNoticeServiceAllList" resultType="FP_Notice">
		select 
			idKey,
			userIdKey,
			createDate,
			title,
			content,
			viewCount,
			startDate,
			endDate
		from FP_Notice
		order by createDate ASC
	</select>
	
	<select id="selectNoticeServiceList" resultType="FP_Notice">
		select 
			idKey,
			userIdKey,
			createDate,
			title,
			content,
			viewCount,
			startDate,
			endDate
		from FP_Notice
		order by createDate ASC
	</select>
	
</mapper>